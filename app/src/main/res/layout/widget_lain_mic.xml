<?xml version="1.0" encoding="utf-8"?>
<!--
    The 1x1: nothing but the glyph. Sits on a home screen like a launcher icon but
    goes straight into listening instead of opening the app.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_button"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="4dp">

    <ImageView
        android:id="@+id/widget_icon"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/ic_lain_glyph"
        android:contentDescription="Talk to Lain"
        android:adjustViewBounds="true" />

</LinearLayout>
